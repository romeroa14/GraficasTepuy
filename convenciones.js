// Camel Case para variables
var miVariable

// Mayúsculas para constantes
const URL = ''

// Camel Case para nombres de funciones
function miFuncion(){}

// Camel Case para propiedades de objetos
const persona = {
    nombreCompleto: "John Doe",
    edad: 30,
    correoElectronico: "john@example.com"
}

// Pascal Case para nombres de clases
class Usuario {
    constructor(nombre, edad) {
        this.nombre = nombre;
        this.edad = edad;
    }
}

// Mayúsculas para nombres de enumeraciones
const DIAS_DE_LA_SEMANA = ["Lunes", "Martes", "Miércoles", "Jueves", "Viernes", "Sábado", "Domingo"];

// Camel Case para nombres de métodos y eventos
button.addEventListener("click", handleClickEvent);
function calcularPromedio(listaDeNumeros) {}

// Snake Case o Kebab Case para nombres de archivos
// mi_archivo_util.js o miArchivoUtil.js
import miModulo from "mi-modulo";
